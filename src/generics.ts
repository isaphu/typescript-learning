const names = ['Isa', 'Jack'];

/*

generic type "Array<t>" is a type
which is kind of connected with 
some other types and its really flexible 
regarding which exactly type that other type is. 

*/

const names: Array = [];